{
  "name": "astra-ai",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "concurrently -n api,web -c cyan,green \"npm run dev:api\" \"npm run dev:web\"",
    "dev:api": "uv run --project apps/api uvicorn --app-dir apps/api/src astra_api.main:app --reload --host 0.0.0.0 --port 8010",
    "dev:web": "npm --prefix apps/web run dev",
    "test:api": "uv run --project apps/api pytest",
    "validate:openspec": "openspec validate connect-web-demo-to-backend --strict"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
